# PRD — S-Semi 반도체 시료 생산주문관리 시스템

## 1. 배경 및 목적

S-Semi는 반도체 시료(Sample)를 연구소·팹리스 업체·대학 연구실에 납품하는 가상의 반도체 회사다.  
주문량 급증으로 엑셀·메모장 기반 관리의 한계(주문 누락, 중복 생산, 재고 불일치)가 드러났고,  
이를 해결하기 위해 콘솔 기반 생산주문관리 시스템을 개발한다.

---

## 2. 역할(Role)

| 역할 | 설명 |
|------|------|
| **주문 담당자** | 고객 요청을 받아 주문서를 작성하고, 출고 처리를 수행한다 |
| **생산 담당자** | 시료를 등록하고, 주문을 승인/거절하며, 생산 완료를 처리한다 |

> 고객은 시스템 외부 행위자(이메일 요청)이며 시스템에 직접 접근하지 않는다.

---

## 3. 주문 상태 머신

```
[주문 담당자]
    주문 등록 → RESERVED

[생산 담당자]
    RESERVED + 거절 → REJECTED          ※ 이후 모니터링 제외
    RESERVED + 승인 + 재고 충분 → CONFIRMED
    RESERVED + 승인 + 재고 부족 → PRODUCING

[생산 라인]
    PRODUCING + 생산 완료 → CONFIRMED    ※ 재고 1 증가 후 차감

[주문 담당자]
    CONFIRMED → RELEASED
```

| 상태 | 의미 |
|------|------|
| `RESERVED` | 주문 접수, 생산 담당자 승인 대기 |
| `REJECTED` | 주문 거절 (모니터링 제외) |
| `PRODUCING` | 승인 완료, 재고 부족으로 생산 라인 투입 중 |
| `CONFIRMED` | 출고 대기 (재고 차감 완료) |
| `RELEASED` | 출고 완료 |

---

## 4. 기능 요구사항

### 4-1. 주문 담당자 메뉴

| ID | 기능 | 설명 |
|----|------|------|
| OM-01 | 주문 등록 | 시료 ID·수량을 입력해 RESERVED 상태의 주문을 생성한다 |
| OM-02 | 주문 목록 조회 | REJECTED를 제외한 전체 주문 목록을 상태·ID 순으로 출력한다 |
| OM-03 | 출고 처리 | CONFIRMED 상태의 주문을 RELEASED로 전환한다 |

### 4-2. 생산 담당자 메뉴

| ID | 기능 | 설명 |
|----|------|------|
| PM-01 | 시료 등록 | 시료 이름·초기 재고 수량을 입력해 시료를 등록한다 |
| PM-02 | 시료 목록 조회 | 등록된 시료 ID·이름·현재 재고를 출력한다 |
| PM-03 | 주문 승인 | RESERVED 주문을 승인한다. 재고가 충분하면 CONFIRMED, 부족하면 PRODUCING으로 전환한다 |
| PM-04 | 주문 거절 | RESERVED 주문을 REJECTED로 전환한다 |
| PM-05 | 생산 현황 조회 | 생산 라인에 투입된 PRODUCING 주문 목록을 출력한다 |
| PM-06 | 생산 완료 처리 | PRODUCING 상태의 주문을 CONFIRMED으로 전환하고 재고를 갱신한다 |

### 4-3. 공통

| ID | 기능 | 설명 |
|----|------|------|
| CM-01 | 역할 선택 | 프로그램 시작 시 역할(주문 담당자 / 생산 담당자)을 선택한다 |
| CM-02 | 역할 전환 | 메인 메뉴로 돌아가 역할을 재선택할 수 있다 |
| CM-03 | 데이터 로드 | 프로그램 시작 시 JSON 파일에서 시료·주문 데이터를 읽어 복원한다 |
| CM-04 | 데이터 저장 | 상태를 변경하는 모든 작업 직후 JSON 파일에 자동 저장한다 |

---

## 5. 비기능 요구사항

| 항목 | 내용 |
|------|------|
| 언어 및 표준 | C++20 |
| 빌드 환경 | Visual Studio 2022, MSVC v145, x64 |
| 인터페이스 | 콘솔(표준 입출력). GUI 없음 |
| 영속성 | JSON 파일 저장. 프로그램 재시작 후에도 데이터 유지 |
| JSON 라이브러리 | [nlohmann/json](https://github.com/nlohmann/json) 단일 헤더(`json.hpp`) |
| 동시성 | 단일 스레드. 생산 라인은 시뮬레이션(수동 완료 처리) |

---

## 6. 도메인 모델

```
Sample
  - id        : int          (자동 부여, 1부터 증가)
  - name      : string
  - stock     : int          (현재 재고 수량, 0 이상)

Order
  - id        : int          (자동 부여, 1부터 증가)
  - sampleId  : int
  - quantity  : int          (1 이상)
  - status    : OrderStatus  (RESERVED | REJECTED | PRODUCING | CONFIRMED | RELEASED)

ProductionLine
  - 처리 대상 : PRODUCING 상태 주문 (FIFO)
  - 생산 단위 : 주문 1건씩 처리
  - 완료 처리 : 생산 담당자가 수동으로 완료 입력
```

---

## 7. 재고 처리 규칙

- **승인 시 재고 확인**: `sample.stock >= order.quantity` 이면 재고 충분.
- **재고 차감 시점**: 주문이 CONFIRMED으로 전환되는 시점에 `stock -= quantity`.
  - 즉시 CONFIRMED(재고 충분 경로)과 생산 완료 후 CONFIRMED(생산 경로) 모두 이 시점에 차감.
- **중복 생산 방지**: 동일 시료에 대해 재고가 충분한 상태에서 추가 생산을 시작하지 않는다.

---

## 8. 데이터 파일 구조

프로그램 실행 파일과 같은 경로에 저장된다.

### `data/samples.json`
```json
{
  "nextId": 3,
  "samples": [
    { "id": 1, "name": "GaN-100", "stock": 10 },
    { "id": 2, "name": "SiC-200", "stock": 0 }
  ]
}
```

### `data/orders.json`
```json
{
  "nextId": 4,
  "orders": [
    { "id": 1, "sampleId": 1, "quantity": 3, "status": "CONFIRMED" },
    { "id": 2, "sampleId": 2, "quantity": 5, "status": "PRODUCING" },
    { "id": 3, "sampleId": 1, "quantity": 2, "status": "RESERVED" }
  ]
}
```

- `nextId`: 다음 엔티티에 부여할 ID. 재시작 시 중복 ID 방지.
- 상태 값은 문자열(`"RESERVED"` 등)로 직렬화한다.
- 저장 시점: 등록·승인·거절·생산 완료·출고 처리 직후 즉시 덮어쓴다.

---

## 9. 콘솔 UI 흐름 (개요)

```
프로그램 시작 → JSON 로드
  └─ [역할 선택]
       ├─ 1. 주문 담당자
       │    ├─ 1. 주문 등록
       │    ├─ 2. 주문 목록 조회
       │    ├─ 3. 출고 처리
       │    └─ 0. 역할 재선택
       └─ 2. 생산 담당자
            ├─ 1. 시료 등록
            ├─ 2. 시료 목록 조회
            ├─ 3. 주문 승인
            ├─ 4. 주문 거절
            ├─ 5. 생산 현황 조회
            ├─ 6. 생산 완료 처리
            └─ 0. 역할 재선택
```

---

## 10. 범위 외 (Out of Scope)

- 고객의 직접 시스템 접근
- 네트워크·멀티유저 지원
- 자동 생산(생산 라인 스레드 자동화)
- 데이터 암호화 및 접근 권한 관리
